/**export const DefaultValueProvider = Symbol.for('DefaultValueProvider');

export interface DefaultValueProvider {
    provide(type: string, name: string): any;

    canHandle(type: string, name: string): boolean;
}
**/
